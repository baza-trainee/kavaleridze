import { FC } from 'react';
import Form from '../Form/Form';

const Home: FC = () => {
  return (
    <div style={{ height: '900px', backgroundColor: 'white' }}>
      <Form />
    </div>
  );
};

export default Home;
